# To get started with Dependabot version updates, you'll need to specify which
# package ecosystems to update and where the package manifests are located.
# Please see the documentation for all configuration options:
# https://docs.github.com/code-security/dependabot/dependabot-version-updates/configuration-options-for-the-dependabot.yml-file

version: 2
updates:
  # Python packages in the backend
  - package-ecosystem: "pip"
    directory: "/backend" # Adjust if your Python package manifests are located elsewhere
    schedule:
      interval: "weekly"

  # JavaScript packages in the frontend
  - package-ecosystem: "npm"
    directory: "/frontend" # Adjust if your JavaScript package manifests are located elsewhere
    schedule:
      interval: "weekly"
